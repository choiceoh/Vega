# QMD 2.0 설정 — 모델은 자동 다운로드됨 (HuggingFace)
# 기본 모델: Qwen3-Embedding-8B Q4, qwen3-reranker-0.6b, LFM2-1.2B-Q4_K_M (쿼리 확장)
# 한국어 컬렉션은 QMD_EMBED_MODEL 환경변수로 Qwen3-Embedding 사용 권장

[search]
candidate_limit = 40
min_score = 0.3

[llm]
disposeModelsOnInactivity = true
inactivityTimeoutMs = 300000
